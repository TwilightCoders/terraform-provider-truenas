---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "truenas_cloudsync_credentials Resource - truenas"
subcategory: ""
description: |-
  Manages credentials for cloud sync tasks. Set exactly one storage type under storage. Secret fields are sensitive but are stored in Terraform state, because TrueNAS reports them back.
---

# truenas_cloudsync_credentials (Resource)

Manages credentials for cloud sync tasks. Set exactly one storage type under `storage`. Secret fields are sensitive but are stored in Terraform state, because TrueNAS reports them back.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable name for the cloud credential.
- `storage` (Attributes) Cloud provider configuration including type and authentication details. Set exactly one of the nested attributes. (see [below for nested schema](#nestedatt--storage))

### Read-Only

- `id` (Number) Identifier assigned by TrueNAS.

<a id="nestedatt--storage"></a>
### Nested Schema for `storage`

Optional:

- `azureblob` (Attributes) Settings when `type` is `AZUREBLOB`. (see [below for nested schema](#nestedatt--storage--azureblob))
- `b2` (Attributes) Settings when `type` is `B2`. (see [below for nested schema](#nestedatt--storage--b2))
- `box` (Attributes) Settings when `type` is `BOX`. (see [below for nested schema](#nestedatt--storage--box))
- `dropbox` (Attributes) Settings when `type` is `DROPBOX`. (see [below for nested schema](#nestedatt--storage--dropbox))
- `ftp` (Attributes) Settings when `type` is `FTP`. (see [below for nested schema](#nestedatt--storage--ftp))
- `google_cloud_storage` (Attributes) Settings when `type` is `GOOGLE_CLOUD_STORAGE`. (see [below for nested schema](#nestedatt--storage--google_cloud_storage))
- `google_drive` (Attributes) Settings when `type` is `GOOGLE_DRIVE`. (see [below for nested schema](#nestedatt--storage--google_drive))
- `google_photos` (Attributes) Settings when `type` is `GOOGLE_PHOTOS`. (see [below for nested schema](#nestedatt--storage--google_photos))
- `http` (Attributes) Settings when `type` is `HTTP`. (see [below for nested schema](#nestedatt--storage--http))
- `hubic` (Attributes) Settings when `type` is `HUBIC`. (see [below for nested schema](#nestedatt--storage--hubic))
- `mega` (Attributes) Settings when `type` is `MEGA`. (see [below for nested schema](#nestedatt--storage--mega))
- `onedrive` (Attributes) Settings when `type` is `ONEDRIVE`. (see [below for nested schema](#nestedatt--storage--onedrive))
- `openstack_swift` (Attributes) Settings when `type` is `OPENSTACK_SWIFT`. (see [below for nested schema](#nestedatt--storage--openstack_swift))
- `pcloud` (Attributes) Settings when `type` is `PCLOUD`. (see [below for nested schema](#nestedatt--storage--pcloud))
- `s3` (Attributes) Settings when `type` is `S3`. (see [below for nested schema](#nestedatt--storage--s3))
- `sftp` (Attributes) Settings when `type` is `SFTP`. (see [below for nested schema](#nestedatt--storage--sftp))
- `storj_ix` (Attributes) Settings when `type` is `STORJ_IX`. (see [below for nested schema](#nestedatt--storage--storj_ix))
- `webdav` (Attributes) Settings when `type` is `WEBDAV`. (see [below for nested schema](#nestedatt--storage--webdav))
- `yandex` (Attributes) Settings when `type` is `YANDEX`. (see [below for nested schema](#nestedatt--storage--yandex))

<a id="nestedatt--storage--azureblob"></a>
### Nested Schema for `storage.azureblob`

Required:

- `account` (String) Azure Blob Storage account name for authentication.
- `key` (String, Sensitive) Azure Blob Storage access key for authentication.

Optional:

- `endpoint` (String) Custom Azure Blob Storage endpoint URL. Empty string for default endpoints. Defaults to `""`.


<a id="nestedatt--storage--b2"></a>
### Nested Schema for `storage.b2`

Required:

- `account` (String) Backblaze B2 account ID for authentication.
- `key` (String, Sensitive) Backblaze B2 application key for authentication.


<a id="nestedatt--storage--box"></a>
### Nested Schema for `storage.box`

Required:

- `token` (String, Sensitive) Box OAuth access token for API authentication.

Optional:

- `client_id` (String) Box OAuth application client ID. Defaults to `""`.
- `client_secret` (String, Sensitive) Box OAuth application client secret. Defaults to `""`.


<a id="nestedatt--storage--dropbox"></a>
### Nested Schema for `storage.dropbox`

Required:

- `token` (String, Sensitive) Dropbox OAuth access token for API authentication.

Optional:

- `client_id` (String) Dropbox OAuth application client ID. Defaults to `""`.
- `client_secret` (String, Sensitive) Dropbox OAuth application client secret. Defaults to `""`.


<a id="nestedatt--storage--ftp"></a>
### Nested Schema for `storage.ftp`

Required:

- `host` (String) FTP server hostname or IP address.
- `pass` (String, Sensitive) FTP password for authentication.
- `user` (String) FTP username for authentication.

Optional:

- `port` (Number) FTP server port number. Defaults to `21`.


<a id="nestedatt--storage--google_cloud_storage"></a>
### Nested Schema for `storage.google_cloud_storage`

Required:

- `service_account_credentials` (String) JSON service account credentials for Google Cloud Storage authentication.


<a id="nestedatt--storage--google_drive"></a>
### Nested Schema for `storage.google_drive`

Required:

- `token` (String, Sensitive) OAuth access token for Google Drive authentication.

Optional:

- `client_id` (String) OAuth client ID for Google Drive API access. Defaults to `""`.
- `client_secret` (String, Sensitive) OAuth client secret for Google Drive API access. Defaults to `""`.
- `team_drive` (String) Google Drive team drive ID or empty string for personal drive. Defaults to `""`.


<a id="nestedatt--storage--google_photos"></a>
### Nested Schema for `storage.google_photos`

Required:

- `token` (String, Sensitive) OAuth access token for Google Photos authentication.

Optional:

- `client_id` (String) OAuth client ID for Google Photos API access. Defaults to `""`.
- `client_secret` (String, Sensitive) OAuth client secret for Google Photos API access. Defaults to `""`.


<a id="nestedatt--storage--http"></a>
### Nested Schema for `storage.http`

Required:

- `url` (String) HTTP URL for file access.


<a id="nestedatt--storage--hubic"></a>
### Nested Schema for `storage.hubic`

Required:

- `token` (String, Sensitive) OAuth access token for Hubic authentication.


<a id="nestedatt--storage--mega"></a>
### Nested Schema for `storage.mega`

Required:

- `pass` (String, Sensitive) MEGA password for authentication.
- `user` (String) MEGA username for authentication.


<a id="nestedatt--storage--onedrive"></a>
### Nested Schema for `storage.onedrive`

Required:

- `drive_id` (String) OneDrive drive identifier.
- `drive_type` (String) Type of OneDrive to access.
- `token` (String, Sensitive) OAuth access token for OneDrive authentication.

Optional:

- `client_id` (String) OAuth client ID for OneDrive API access. Defaults to `""`.
- `client_secret` (String, Sensitive) OAuth client secret for OneDrive API access. Defaults to `""`.


<a id="nestedatt--storage--openstack_swift"></a>
### Nested Schema for `storage.openstack_swift`

Required:

- `auth` (String) Swift authentication URL endpoint.
- `auth_version` (Number) Swift authentication API version.

* `0`: Legacy auth
* `1`: TempAuth
* `2`: Keystone v2.0
* `3`: Keystone v3
* `null`: Auto-detect
- `endpoint_type` (String) Swift endpoint type to use.

* `public`: Public endpoint (default)
* `internal`: Internal network endpoint
* `admin`: Administrative endpoint
* `null`: Use default
- `key` (String, Sensitive) Swift password or API key for authentication.
- `user` (String) Swift username for authentication.

Optional:

- `application_credential_id` (String) Swift application credential ID for authentication. Defaults to `""`.
- `application_credential_name` (String) Swift application credential name for authentication. Defaults to `""`.
- `application_credential_secret` (String, Sensitive) Swift application credential secret for authentication. Defaults to `""`.
- `auth_token` (String, Sensitive) Swift authentication token for pre-authenticated access. Defaults to `""`.
- `domain` (String) Swift domain name for authentication. Defaults to `""`.
- `region` (String) Swift region name for geographic distribution. Defaults to `""`.
- `storage_url` (String) Swift storage URL endpoint. Defaults to `""`.
- `tenant` (String) Swift tenant name for multi-tenancy. Defaults to `""`.
- `tenant_domain` (String) Swift tenant domain name. Defaults to `""`.
- `tenant_id` (String) Swift tenant ID for multi-tenancy. Defaults to `""`.
- `user_id` (String) Swift user ID for authentication. Defaults to `""`.


<a id="nestedatt--storage--pcloud"></a>
### Nested Schema for `storage.pcloud`

Required:

- `token` (String, Sensitive) OAuth access token for pCloud authentication.

Optional:

- `client_id` (String) OAuth client ID for pCloud API access. Defaults to `""`.
- `client_secret` (String, Sensitive) OAuth client secret for pCloud API access. Defaults to `""`.
- `hostname` (String) pCloud hostname or empty string for default. Defaults to `""`.


<a id="nestedatt--storage--s3"></a>
### Nested Schema for `storage.s3`

Required:

- `access_key_id` (String) S3 access key ID for authentication.
- `secret_access_key` (String, Sensitive) S3 secret access key for authentication.

Optional:

- `endpoint` (String) S3-compatible endpoint URL or empty string for AWS S3. Defaults to `""`.
- `max_upload_parts` (Number) Maximum number of parts for multipart uploads. Defaults to `10000`.
- `region` (String) S3 region or empty string for default. Defaults to `""`.
- `signatures_v2` (Boolean) Whether to use AWS Signature Version 2. Defaults to `false`.
- `skip_region` (Boolean) Whether to skip region validation. Defaults to `false`.


<a id="nestedatt--storage--sftp"></a>
### Nested Schema for `storage.sftp`

Required:

- `host` (String) SFTP server hostname or IP address.
- `user` (String) SFTP username for authentication.

Optional:

- `pass` (String, Sensitive) SFTP password for authentication or `null` for key-based auth.
- `port` (Number) SFTP server port number. Defaults to `22`.
- `private_key` (Number) SSH private key ID for authentication or `null` for password auth.


<a id="nestedatt--storage--storj_ix"></a>
### Nested Schema for `storage.storj_ix`

Required:

- `access_key_id` (String) Storj S3-compatible access key ID for authentication.
- `secret_access_key` (String, Sensitive) Storj S3-compatible secret access key for authentication.

Optional:

- `endpoint` (String) Storj gateway endpoint URL for S3-compatible access. Defaults to `"https://gateway.storjshare.io/"`.


<a id="nestedatt--storage--webdav"></a>
### Nested Schema for `storage.webdav`

Required:

- `pass` (String, Sensitive) WebDAV password for authentication.
- `url` (String) WebDAV server URL endpoint.
- `user` (String) WebDAV username for authentication.
- `vendor` (String) WebDAV server vendor type for compatibility optimizations.

* `NEXTCLOUD`: Nextcloud server
* `OWNCLOUD`: ownCloud server
* `SHAREPOINT`: Microsoft SharePoint
* `OTHER`: Generic WebDAV server


<a id="nestedatt--storage--yandex"></a>
### Nested Schema for `storage.yandex`

Required:

- `token` (String, Sensitive) Yandex OAuth access token for API authentication.

Optional:

- `client_id` (String) Yandex OAuth application client ID. Defaults to `""`.
- `client_secret` (String, Sensitive) Yandex OAuth application client secret. Defaults to `""`.

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to       = truenas_cloudsync_credentials.example
  provider = truenas
  identity = {
    id = 1
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `id` (Number) Identifier assigned by TrueNAS.

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import by TrueNAS id
terraform import truenas_cloudsync_credentials.example 1
```
